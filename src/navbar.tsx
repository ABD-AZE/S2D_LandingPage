import "./navbar.css";
const Navbar = () => {
  return (
    <nav>
      <button>HOME</button>
      <button>ABOUT</button>
      <button>FAQ</button>
      <button>CONTACT</button>
    </nav>
  );
};

export default Navbar;
